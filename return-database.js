document.addEventListener('DOMContentLoaded',async()=>{
 const auth=window.actionBuyerAuth,list=document.getElementById('return-database-list'),summary=document.getElementById('return-database-summary');if(!auth||!list)return;const session=await auth.getSession();if(!session){location.href='login.html?return=return-database.html';return;}const db=auth.supabase;const {data:staff}=await db.from('staff_users').select('user_id').eq('user_id',session.user.id).maybeSingle();if(!staff){list.innerHTML='<p>No permission.</p>';return;}
 const {data:rows,error}=await db.from('inventory_return_data').select('*').order('authorised_at',{ascending:false});if(error){list.innerHTML=`<p>${escapeHtml(error.message)}</p>`;return;}const rs=rows||[];const money=n=>Number(n||0).toLocaleString('en-GB',{style:'currency',currency:'GBP'});const date=v=>v?new Date(v).toLocaleString('en-GB'):'Not recorded';
 summary.innerHTML=`<div><strong>${rs.length}</strong><br>authorised return records</div>`;if(!rs.length){list.innerHTML='<div class="empty-account"><h3>No authorised returns yet</h3><p>A return enters this database after the item has been received and staff authorise the return.</p></div>';return;}
 list.innerHTML=rs.map(r=>{const p=r.product_snapshot||{},f=r.financial_snapshot||{},c=r.customer_snapshot||{},photos=Array.isArray(r.photo_snapshot)?r.photo_snapshot:[];return `<details class="valuation-card" style="margin-bottom:1rem"><summary style="cursor:pointer;list-style:none"><div style="display:grid;grid-template-columns:minmax(220px,2fr) repeat(3,minmax(120px,1fr));gap:.75rem;align-items:center"><div><p class="section-kicker">RETURN AUTHORISED</p><strong>${escapeHtml([p.manufacturer,p.model].filter(Boolean).join(' ')||'Product')}</strong><br><small>${escapeHtml(r.return_reference)} · ${escapeHtml(r.transaction_number)}</small></div><div><strong>Purchase</strong><br>${money(f.purchase_price)}</div><div><strong>Sold</strong><br>${money(f.sold_price)}</div><div><strong>Authorised</strong><br>${escapeHtml(date(r.authorised_at))}</div></div></summary><div style="margin-top:1rem;padding-top:1rem;border-top:1px solid #ddd"><div class="notice"><div style="display:grid;grid-template-columns:repeat(auto-fit,minmax(180px,1fr));gap:.6rem"><p><strong>Transaction</strong><br>${escapeHtml(r.transaction_number)}</p><p><strong>Return reference</strong><br>${escapeHtml(r.return_reference)}</p><p><strong>Manufacturer / model</strong><br>${escapeHtml(p.manufacturer||'')} ${escapeHtml(p.model||'')}</p><p><strong>Package</strong><br>${escapeHtml(p.package_name||'Not recorded')}</p><p><strong>Customer condition</strong><br>${escapeHtml(p.customer_condition||'Not recorded')}</p><p><strong>Staff condition</strong><br>${escapeHtml(p.staff_condition||'Not recorded')}</p><p><strong>Purchase cost</strong><br>${money(f.purchase_price)}</p><p><strong>Sold for</strong><br>${money(f.sold_price)}</p><p><strong>Sold channel</strong><br>${escapeHtml(f.sold_channel||'Not recorded')}</p></div><p><strong>Customer reason:</strong> ${escapeHtml(c.reason||'Not recorded')}</p><p><strong>Customer notes:</strong> ${escapeHtml(c.customer_notes||'None')}</p></div><details style="margin-top:1rem"><summary style="cursor:pointer"><strong>Photos (${photos.length})</strong></summary>${photos.length?`<div style="margin-top:1rem"><p>Original evidence paths retained in the permanent return record.</p>${photos.map(x=>`<p>${escapeHtml(x.file_url||x.path||'Photo')}</p>`).join('')}</div>`:'<p>No photos recorded.</p>'}</details><details style="margin-top:1rem"><summary style="cursor:pointer"><strong>Inspection / testing snapshot</strong></summary><pre style="white-space:pre-wrap;overflow:auto">${escapeHtml(JSON.stringify(r.inspection_snapshot||[],null,2))}</pre></details><details style="margin-top:1rem"><summary style="cursor:pointer"><strong>Cost snapshot</strong></summary><pre style="white-space:pre-wrap;overflow:auto">${escapeHtml(JSON.stringify(r.expense_snapshot||[],null,2))}</pre></details></div></details>`;}).join('');
 function escapeHtml(v){return String(v??'').replaceAll('&','&amp;').replaceAll('<','&lt;').replaceAll('>','&gt;').replaceAll('"','&quot;').replaceAll("'",'&#039;');}
});
