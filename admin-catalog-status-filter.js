/* GearCashOut catalogue status filter + automatic online comparison fallback. */
(function(){
  'use strict';
  const money=value=>Number(value).toLocaleString('en-GB',{style:'currency',currency:'GBP'});
  const comparisonCache=new Map(); let timer=null;
  const isConsumerPrice=row=>{const notes=String(row?.notes||'');if(/\binc\.?\s*vat\b|including\s+vat|vat\s+included/i.test(notes))return true;return !(/\bex\.?\s*vat\b|excluding\s+vat|plus\s+vat|vat\s+excluded/i.test(notes));};
  const marketplaceAuctionPatterns=[/(^|\.)ebay\./i,/(^|\.)vinted\./i,/(^|\.)facebook\.com/i,/(^|\.)gumtree\./i,/(^|\.)etsy\./i,/(^|\.)depop\./i,/(^|\.)shpock\./i,/(^|\.)preloved\./i,/(^|\.)catawiki\./i,/(^|\.)bidspotter\./i,/(^|\.)thesaleroom\./i,/(^|\.)liveauctioneers\./i,/(^|\.)invaluable\./i,/(^|\.)barnebys\./i,/(^|\.)auctionzip\./i,/(^|\.)copart\./i];
  function isDirectRetailer(row){const haystack=`${String(row?.retailer||'').trim().toLowerCase()} ${String(row?.source_url||'').trim().toLowerCase()}`;return !marketplaceAuctionPatterns.some(p=>p.test(haystack));}
  function applyStatusFilter(){const filter=document.getElementById('status-filter'),list=document.getElementById('catalog-list');if(!filter||!list)return;const selected=filter.value,cards=list.querySelectorAll('.valuation-card');cards.forEach(card=>{const badge=card.querySelector('.status-badge'),titleStatus=card.querySelector('.catalog-title-status'),status=(badge?.textContent||titleStatus?.textContent||'').trim().toLowerCase();card.style.display=(!selected||status===selected)?'':'none';});const visible=Array.from(cards).some(card=>card.style.display!=='none');let empty=list.querySelector('.status-filter-empty');if(selected&&cards.length&&!visible){if(!empty){empty=document.createElement('div');empty.className='empty-account status-filter-empty';list.appendChild(empty);}empty.innerHTML=`<h3>No ${selected} products found</h3><p>Change the Status filter or choose All products.</p>`;empty.style.display='';}else if(empty)empty.style.display='none';}
  function productId(card){return card?.dataset?.productId||card?.querySelector('.edit-product')?.dataset?.id||card?.querySelector('.catalog-title-status')?.dataset?.id||card?.querySelector('.catalog-inline-edit')?.dataset?.id||'';}
  async function loadAllComparisons(){const supabase=window.actionBuyerAuth?.supabase;if(!supabase)return;const byProduct=new Map();let from=0;const pageSize=1000;while(true){const {data,error}=await supabase.from('quote_catalog_retailer_prices').select('catalog_product_id,retailer,sell_price,availability_status,notes,price_type,source_url').in('price_type',['new','new_sale']).range(from,from+pageSize-1);if(error){console.error('Online comparison price lookup failed',error);return;}(data||[]).forEach(row=>{if(!isDirectRetailer(row)||row.sell_price==null||!isConsumerPrice(row))return;const availability=String(row.availability_status||'').toLowerCase();if(availability==='out_of_stock'){if(/\bdiscontinued\b/i.test(String(row.notes||'')))return;}else if(!['in_stock','unknown'].includes(availability))return;const price=Number(row.sell_price);if(!Number.isFinite(price)||price<=0||!row.catalog_product_id)return;if(!byProduct.has(row.catalog_product_id))byProduct.set(row.catalog_product_id,[]);byProduct.get(row.catalog_product_id).push(price);});if(!data||data.length<pageSize)break;from+=pageSize;}byProduct.forEach((prices,id)=>{const sorted=[...new Set(prices)].sort((a,b)=>a-b);comparisonCache.set(id,sorted.length===1?money(sorted[0]):`${money(sorted[0])}–${money(sorted[sorted.length-1])}`);});processCatalogue();}
  function renderComparison(card){const id=productId(card),comparison=id?comparisonCache.get(id):null;if(!id||!comparison)return;card.dataset.onlineComparison=comparison;const title=card.querySelector('.catalog-accordion-title p')||card.querySelector('.valuation-card > div:first-child p');if(title){const text=title.textContent,marker=' · Sealed ',suffix=text.includes(marker)?text.slice(text.indexOf(marker)):'';const next=`Online comparison ${comparison}${suffix}`;if(/^RRP\s+—/i.test(text.trim())||/^Online comparison\s+/i.test(text.trim()))title.textContent=next;}}
  function processCatalogue(){const list=document.getElementById('catalog-list');if(!list)return;list.querySelectorAll('.valuation-card').forEach(renderComparison);}
  function schedule(){clearTimeout(timer);timer=setTimeout(processCatalogue,300);}
  function wire(){const filter=document.getElementById('status-filter'),list=document.getElementById('catalog-list');if(!filter||!list)return;if(list.dataset.statusFilterWired!=='1'){list.dataset.statusFilterWired='1';filter.addEventListener('change',applyStatusFilter);const observer=new MutationObserver(mutations=>{const structural=mutations.some(m=>Array.from(m.addedNodes).some(node=>node.nodeType===1&&!node.classList.contains('online-comparison-stat')));if(structural){applyStatusFilter();schedule();}});observer.observe(list,{childList:true});}applyStatusFilter();schedule();setTimeout(schedule,1000);setTimeout(schedule,2500);loadAllComparisons();}
  document.addEventListener('DOMContentLoaded',wire,{once:true});
})();