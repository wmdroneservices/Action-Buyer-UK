document.addEventListener("DOMContentLoaded",async()=>{const auth=window.actionBuyerAuth;if(!auth)return;const session=await auth.getSession();if(!session)return;const{data:sales}=await auth.supabase.from("sales").select("id,sale_reference,status,total_amount,created_at").eq("user_id",session.user.id).order("created_at",{ascending:false});if(!sales?.length)return;const ids=sales.map(s=>s.id);const{data:items}=await auth.supabase.from("sale_items").select("sale_id,quote_item_id,amount").in("sale_id",ids);const qids=(items||[]).map(i=>i.quote_item_id);const{data:qitems}=qids.length?await auth.supabase.from("quote_items").select("id,model,manufacturer,item_name").in("id",qids):{data:[]};const{data:shipments}=await auth.supabase.from("shipments").select("sale_id,shipment_type,status,carrier,tracking_number,parcel_count,label_count,label_urls").in("sale_id",ids).order("created_at",{ascending:false});const money=n=>new Intl.NumberFormat("en-GB",{style:"currency",currency:"GBP"}).format(Number(n||0));const esc=v=>String(v??"").replaceAll("&","&amp;").replaceAll("<","&lt;").replaceAll(">","&gt;").replaceAll('"',"&quot;").replaceAll("'","&#039;");const section=document.createElement("section");section.className="account-panel";section.innerHTML='<div class="section-heading"><p class="section-kicker">PURCHASES</p><h2>Purchase summary</h2><p>Accepted items are combined into one sale and total.</p></div>'+sales.map(s=>{const si=(items||[]).filter(i=>i.sale_id===s.id);const sh=(shipments||[]).filter(x=>x.sale_id===s.id);return `<article class="valuation-card"><div><span class="valuation-ref">${esc(s.sale_reference)}</span><p class="section-kicker">${esc(s.status.replaceAll("_"," "))}</p><h3>${money(s.total_amount)}</h3><p>${si.map(i=>{const q=(qitems||[]).find(x=>x.id===i.quote_item_id);return esc(q?.model||q?.item_name||"Item")+" — "+money(i.amount);}).join("<br>")}</p></div><div class="valuation-meta">${sh.length?sh.map(x=>`<span class="status-badge">${esc(x.shipment_type.replaceAll("_"," "))}: ${esc(x.status)} — ${esc(x.label_count)} label(s) / ${esc(x.parcel_count)} parcel(s)${x.tracking_number?" — "+esc(x.tracking_number):""}</span>`).join("<br>"):"<small>Shipping instructions will appear here when ready.</small>"}</div></article>`;}).join("");document.querySelector("main.account-page .container")?.appendChild(section);});