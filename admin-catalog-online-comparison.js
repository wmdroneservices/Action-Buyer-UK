/* Deprecated compatibility shim.
   The catalogue comparison is now rendered exclusively by
   admin-catalog-market-structure.js, which enforces UK + GBP + NEW/NEW SALE only.
   This file intentionally performs no price calculation so legacy code cannot
   reintroduce overseas prices into Online comparison.
*/
(function(){
  'use strict';
})();
